[package]
name = "cat"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "cat"
test = false
bench = false

[package.metadata.cargo-xbuild]
target = "riscv64gc-unknown-none-elf"

[dependencies]
usys = { path = "../usys" }
uapi = { path = "../uapi" }
