[workspace]
members = ["kernel", "userapp", "cat", "uapi", "usys"]
resolver = "2"

[profile.dev]
panic = "abort"
opt-level = 1
codegen-units = 1
debug = true
lto = false

[profile.release]
panic = "abort"
opt-level = "z"
codegen-units = 1
lto = "fat"
strip = "symbols"

